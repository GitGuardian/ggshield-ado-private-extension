// Arguments for the ggshield command
export const GGSHIELD_ARGS: string[] = ["secret", "scan", "ci"];

// Timeout of the ggshield scan is milliseconds
export const GGSHIELD_SCAN_TIMEOUT_MS: number = 80000;
